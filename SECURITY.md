# 安全与隐私说明

桌面版本面向本机单用户处理有权使用的媒体文件，其本地 HTTP 后端不是互联网服务，也不是安全沙箱。

## 2.1 在线版与安全边界

在线版为 Vercel 静态站点。媒体通过 File API 读入 WebAssembly Worker 内存，无媒体上传接口、第三方脚本或统计代码。托管商仍可能记录网站/引擎请求的 IP、时间与 User-Agent，这不等于媒体内容上传。关闭标签页丢弃文件；结果需要主动下载。

同源 CSP 限制脚本、网络和 Worker；禁用摄像头、麦克风、定位与页面嵌入。文件名只通过 textContent/Option 显示，下载命名清理路径分隔符；限制单文件 128 MiB、输入队列 384 MiB 和 50 个任务，实际内存开销可能更高，尤其是视频解码。取消会销毁 Worker，输出临时文件每次清理。单任务编码设置 5 分钟超时；损坏输入不能保证立即中断底层解析器。

引擎固定为 @ffmpeg/core 0.12.10 / FFmpeg 5.1.4 基础，落后于最新原生 FFmpeg；npm audit 不扫描其编译进去的 C/C++ 库。浏览器隔离减少本机文件暴露，但不是无漏洞保证。请仅处理可信和有权使用的媒体。生产商业服务前须评估新版引擎、完整源码合规、安全审计与专利许可。

桌面新增 OS 文件锁，阻止同一 data-dir 同时启动多个进程；崩溃后锁随进程释放。不需要手工删除 instance.lock。

报告漏洞：优先使用 GitHub Security Advisories（如已启用），否则提交不包含媒体、令牌或本机路径的 Issue，请勿公开可利用的细节。

## 已做的防护

服务只监听 127.0.0.1，不允许通过 --listen 改为 0.0.0.0。每次启动随机会话令牌；API 检查 Bearer、Host 和 Origin；界面不加载 CDN/遥测。FFmpeg 使用独立参数数组调用，不执行由文件名拼接的 shell 命令。输入只允许本地普通文件，FFmpeg 协议白名单限制为 file,pipe，避免把用户媒体路径当网络 URL 拉取。

原文件只读；输出通过不覆盖式提交保留同名原件。JSON 字段、编码参数、文件名后缀、并发数与任务规模受限制；CSV 导出防常见公式前缀注入。外部引擎只有在明确安装确认后才下载，HTTPS 主机与重定向采用白名单，按发行方 SHA-256 校验；限制包体和解压大小；只提取指定程序及许可证文件，不运行下载脚本。

## 不能保证的部分

FFmpeg 是本地原生程序；复杂媒体解析器仍可能有漏洞，当前没有 AppContainer、seccomp、容器或专用账户隔离。只处理可信来源媒体，用普通用户运行，不要以管理员权限批量处理未知文件。没有完成完整渗透测试、独立安全审计或代码签名。

发行方校验文件与包来自同一来源；SHA-256 能发现下载损坏或不匹配，不能抵御发行方账户及两者同时被篡改的情形。本安装器未验证 FFmpeg 官方源码 PGP 签名，不声称第三方二进制受到 FFmpeg 官方签名背书。

导入缓存、解码临时文件、队列路径和元数据存储在本机磁盘，可能包含隐私。正常退出删除本次 session 临时文件，但 imports 和历史队列保留；删除队列项不会替你删除任何媒体。异常退出/断电可能残留临时文件，需确认进程已退出后再清理。不要把整个运行数据目录提交 GitHub。

本次附带程序使用记录在 release-manifest.json 中的构建工具链。正式分发应使用当前受支持的 Go 稳定版重建、执行漏洞扫描，完成 Windows 和引擎验收，再考虑签名发布。已附 GitHub Actions 配置，但并未声称它已经在你的远程仓库执行过。

请勿共享 last-session-url.txt、令牌、含本地路径的日志，勿代理暴露此接口，勿同时用多个进程写同一个 data-dir。商用授权、媒体使用权、编解码器许可与专利问题需按实际分发方案独立审查。
